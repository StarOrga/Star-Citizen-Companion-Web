# PostHog analytics (client-side project token — safe to expose in the browser bundle).
# Copy this file to `.env` (gitignored) for local development, and set the same values
# in your hosting provider's environment variables for production builds.
NG_APP_POSTHOG_PROJECT_TOKEN=
NG_APP_POSTHOG_HOST=https://eu.i.posthog.com
