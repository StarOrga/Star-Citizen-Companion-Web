# Codebase facts for reconciliation — IM VERSE plane

Collected 2026-09-02 while the code-blind agents run. Used in Step 5 to label
each approach (migration path, what survives, effort, risk).

| # | Fact | Where |
|---|------|-------|
| R1 | The landing is one component; the band is the `<section class="versum">` after the `.surface` grid; `.landing` uses a uniform `gap: 20px` between terminal, surface and band. | `src/app/codex/codex-landing.component.ts` (template ~L462–546, styles ~L553, L717–800, L1109–1272) |
| R2 | `.surface`: 1px border `color-mix(accent 18%, border)`, radius 4, `--shadow-elevated`, `overflow: hidden`, 2-col grid (1fr / 1.35fr), single column ≤760px. Each `.zone` has `border-left: 2px solid var(--tint)`; `.zone.board --tint = --sc-warning` (amber), `.zone.hangar --tint = --sc-accent` (cyan); hangar gets `border-top` on mobile, `border-left` on desktop. | same, L717–790 |
| R3 | Eyebrow grammar: `.zone-eyebrow` = display font, 0.68rem, 0.12em tracking, uppercase, `color: var(--tint)`. The band's `.versum-eyebrow` is identical except `color: var(--sc-fg-2)`. A third tint is a one-line change; there is no third accent token in the theme yet (`--sc-accent`, `--sc-accent-hot`, `--sc-warning` exist; concept pages used `--band-violet: #b98bff` as a placeholder). | L789–795, L1112–1118; `src/styles.scss` tokens |
| R4 | Chips: `versumDomains()` computed from `build().entityCounts` (`seeded` preferred), fixed order ship·item·component·weapon·blueprint·manufacturer·ammunition, every chip → `/codex/index?kind=`. Dropping/reordering/grouping = editing one array + template. | L1472–1500 |
| R5 | `/codex/index` (`codex-list.component.ts`) already has a kind bar with counts and a manufacturer `<select>` facet (`manufacturerFacetOptions`), plus per-kind facets (component kind, weapon class, blueprint category). An unfiltered "whole archive" entry = navigating to `/codex/index` without `kind` — needs a check that the list component tolerates a missing kind (today it likely defaults to `ship`); may need a `kind=all` or the poly-search path. | `codex-list.component.ts` L79–170 |
| R6 | Category glyphs: `sc-codex-icon` renders a 24×24 stroke path in `CAT_COLORS[kind]`; colour is baked into the component via `[style.color]`. A monochrome/tinted use needs either a `[color]` input or a CSS override on the host (`sc-codex-icon svg { color: inherit !important }`) — small change. Blueprint has no own glyph (falls back to `generic`). | `codex-category-icon.component.ts` |
| R7 | Board panel vocabulary reusable for staging: `.rdy-ic` = 26×26 field, 1px border, 14px glyph, `.on` = tint border + 15% tint fill + glow; type roles `.t-label` (0.6rem, spaced, uppercase) / `.t-value` (0.78rem) / `.board-name` (1rem). | `codex-board-panel.component.ts` L216–345 |
| R8 | Patch delta data: `CodexService.recentLiveBuilds(2)` returns the two newest LIVE builds incl. `entityCounts` and `patchVersion` — one extra query (already used by `ownedFleetDeltas`). A count delta per kind is pure arithmetic. A "new ships this patch" list would need `shipRowsForBuild` for both builds (~2 queries, 353 rows) — owner chose the QUIET one-liner, so only counts are needed. Renders nothing with <2 builds. | `codex.service.ts` L633–690 |
| R9 | Reißbrett rail: `UpcomingShipsService.feed().ships` (max 20 on the landing), `rsi.notificationCount()` badge, tiles are external `<a href=rsiUrl>`; the internal listing exists at `/codex/upcoming` (route L151). The head link is a one-line template change. | L505–545; `app.routes.ts` L151 |
| R10 | Keybindings link: `<a class="rail-icon" routerLink="/codex/keybinds">` in `.versum-head`; the terminal row (`header.terminal`) already hosts `sc-app-download-menu` on the far right — a natural place for a keybinds tool icon. | L467–480, L86–150 |
| R11 | i18n: `codex.landing.versum.*` keys in `public/i18n/{de,en}.json` (eyebrow, keybinds, domain.*, upcomingRail.*). Rename = `de.json` eyebrow value only. New copy (delta line, archive entry label, group labels) = new keys in both files. | `public/i18n/de.json` L200–216 |
| R12 | Mobile gate: the page must not scroll horizontally; the rail is its own `overflow-x: auto` box; tap targets ≥44px (`--sc-tap-min`), and the gate's emulation measures a 44px target as 43px — use 48px. | memory `sc-mobile-gate-touch-baseline` |
| R13 | Tests: `codex-landing.component.spec.ts` asserts the domain chips are real anchors landing on `/codex/index?kind=…`, that `.versum-rail` is gone, and that the keybinds entry sits in `.versum-head`. Any restructure must update these specs. | spec L422–500 |
| R14 | `/codex/blueprint` (own page) is orphaned since #421 (only the legacy bridge links it) — PO side finding, out of scope here. | `app.routes.ts` L117 |
