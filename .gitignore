# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Angular build
/dist/
/tmp/
/out-tsc/
/bazel-out
.angular/

# Test artifacts
/coverage/
/test-results/
/playwright-report/
/playwright/.cache/
/.playwright-mcp/

# IDE
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
*.swp
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Env / secrets
.env
.env.local
.env.*.local
*.local.json
src/environments/environment.local.ts

# Local release scratch (uploader binary + release token) — never commit
ul-rel/

# Supabase local stack
supabase/.temp/
supabase/.branches/
supabase/.env

# Vercel
.vercel/

# Claude / AI artifacts kept in repo, but exclude session/cache:
.claude/sessions/
.claude/cache/
.claude/scheduled_tasks.lock
.claude/worktrees/
.claude/tmp/
AUTONOMOUS-RESUME.json
AUTONOMOUS-REPORT.html

# Logs
*.log
logs/

# Ship pipeline runtime state (per-process, never commit)
.claude/.ship-in-progress
.claude/.ship-watcher/

# /run-burn + /concept + /claude-batch local-only state
.claude/token-config.json
.claude/concept-active.json
.claude/batch-activity

# Python sidecar (data-uploader) — caches + embedded interpreter + real-P4K subsets
data-uploader/python/__pycache__/
data-uploader/python/**/__pycache__/
data-uploader/python/.pytest_cache/
data-uploader/python/tests/fixtures/real-subset.*
data-uploader/resources/python/
data-uploader/.cache/python-downloads/

# 3D hull/skin export — large build artifacts + fetched external build tools
# (data is 100% P4K; these are generated outputs / downloaded binaries, never commit)
data-uploader/python/demo_out/
data-uploader/python/tools/*.exe
data-uploader/python/tools/*.pdb
data-uploader/python/spike_*.py
data-uploader/python/fs[0-9]*.py
data-uploader/python/real_cutlass_*.py
data-uploader/python/demo_visual_extract.py

# graphify knowledge-graph output (generated locally, never committed)
graphify-out/
